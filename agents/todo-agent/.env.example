# Todo Agent - Environment Variables

PORT=8200

# Primary credential path (copied by deploy.sh when source exists)
FIREBASE_SERVICE_ACCOUNT_KEY=/app/.secrets/serviceAccountKey.json

# If you prefer to reference the key directly without copying, use:
# FIREBASE_SERVICE_ACCOUNT_KEY=/home/ubuntu/app/ai-everyone/serviceAccountKey.json
